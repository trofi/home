#!/bin/bash

for f in /usr/share/fonts/*
do
    echo "adding fontdir '$f'"
    xset +fp "$f"
done
xset fp rehash

PROFILE="${WM:-xmonad}"

CURR_DIR="$RC_DIR"
RC_DIR="$RC_DIR/$PROFILE" \
\
   dbus-launch \
   gpg-agent --daemon \
   ssh-agent \
\
$CURR_DIR/$PROFILE/rc
